language: python
python:
  - '2.7'
  - '3.5'
  - '3.6'
matrix:
  allow_failures:
    - python: '3.5'
    - python: '3.6'

cache: pip
install:
- pip install -U pip
- pip install -U setuptools setuptools_scm pytest
- pip install -e .

script: pytest